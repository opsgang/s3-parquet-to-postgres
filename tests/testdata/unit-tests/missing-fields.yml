db:
s3:
  bucket: "customer-orders-parquet"
  download_batch_size: 2
parquet:
  desired_fields:
    - delivery_id
    - body
work_lists:
  dir: "work"

# missing
# db.conn_str: "host=127.0.0.1 password=postgres user=postgres dbname=warehouse"
# s3.downloads_dir: "out"
